# Constraints for installing vLLM into the uv-managed venv.
#
# vLLM 0.11.0 calls Qwen2Tokenizer.all_special_tokens_extended, which the
# transformers 5.x line removed. Pinning to 4.x lets vLLM launch. Remove
# this constraint once vLLM publishes a release that targets transformers 5.
#
# Usage:
#   uv pip install -c constraints/serve.txt vllm
#
# Validated combinations (record exact versions resolved from this constraint
# at the moment of install in DECISIONS.md):
#   - macOS arm64 (M1):  vllm 0.11.0  +  transformers 4.57.6
#   - linux x86_64 GPU:  vllm 0.11.0  +  transformers 4.57.6   (Phase 7)

vllm>=0.11.0,<0.12
transformers>=4.45,<5.0
